import React, { useState } from "react";
import { Alert } from "react-bootstrap";

const Puertas = () => {
  const [state, setState] = useState(null);
  return <Alert variant="secondary">Puertas mofo</Alert>;
};

export default Puertas;
