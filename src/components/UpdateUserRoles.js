import React, { useEffect, useState } from "react";
import { Alert } from "react-bootstrap";

const UpdateUserRoles = () => {
  const [state, setState] = useState(null);

  useEffect(() => {}, []);
  return <Alert variant="secondary"></Alert>;
};

export default UpdateUserRoles;
