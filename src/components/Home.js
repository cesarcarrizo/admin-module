import React, { useState } from "react";
import { Alert } from "react-bootstrap";

const Home = () => {
  const [state, setState] = useState(null);
  return <>This is home, beautify me later hoe</>;
};

export default Home;
